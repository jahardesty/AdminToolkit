<Page x:Class="AdminToolkit.Pages.ADSearchPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="AD User Search"
      Background="{StaticResource PageBackgroundBrush}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Search Active Directory User" 
                       FontSize="18" 
                       FontWeight="Bold" 
                       VerticalAlignment="Center" 
                       HorizontalAlignment="Left"/>

            <Button Content="?" 
                    Width="30" 
                    Height="30" 
                    HorizontalAlignment="Right" 
                    VerticalAlignment="Center" 
                    Background="#FF757575" 
                    Foreground="White" 
                    FontWeight="Bold" 
                    Cursor="Hand" 
                    ToolTip="Click for instructions" 
                    Click="Help_Click"/>
        </Grid>

        <StackPanel Grid.Row="1">
            <TextBlock Text="Enter SamAccountName (Username):" Margin="0,5,0,5" FontWeight="SemiBold"/>
            <StackPanel Orientation="Horizontal">
                <TextBox x:Name="txtSearchUser" 
                         Width="300" 
                         HorizontalAlignment="Left" 
                         Margin="0,0,10,0" 
                         VerticalContentAlignment="Center" 
                         KeyDown="TxtSearchUser_KeyDown"/>
                <Button Content="Search" Click="SearchUser_Click" Width="100" Height="30" IsDefault="True"/>
            </StackPanel>

            <Separator Margin="0,20,0,20"/>
        </StackPanel>

        <StackPanel Grid.Row="2">
            <TextBlock Text="Distinguished Name (DN):" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBox x:Name="txtResultDN" 
                     IsReadOnly="True" 
                     Background="#EEE" 
                     Padding="10" 
                     TextWrapping="Wrap" 
                     MinHeight="60"/>

            <Button x:Name="btnCopy" 
                    Content="📋 Copy to Clipboard" 
                    HorizontalAlignment="Right" 
                    Margin="0,10,0,0" 
                    Width="150" 
                    Height="30"
                    IsEnabled="False" 
                    Click="CopyDN_Click"/>
        </StackPanel>
    </Grid>
</Page>
